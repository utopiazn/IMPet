<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="PetOrderSQL">

	<sql id="selectAll" > 
		<![CDATA[ SELECT * FROM PET_ITEM_ORDER ]]>
	</sql>
	
	<sql id="where-no"> 
		<![CDATA[ WHERE ORDER_NO = #{ORDER_NO} ]]>
	</sql>
	
	<sql id="where-id"> 
		<![CDATA[ WHERE MEMBER_ID = #{MEMBER_ID} ]]>
	</sql>

	
	<!-- 장바구니 상품 선택 주문 -->
	<select id="selectAll" resultType="hashmap" parameterType="hashmap">
		<![CDATA[
			SELECT a.ITEM_NAME, a.ITEM_IMG, a.ITEM_PRICE, a.ITEM_DCPRICE, a.ITEM_REMAINCOUNT, b.*
				FROM PET_ITEM a, PET_ITEM_BASKET b
					WHERE b.basket_no = #{BASKET_NO} AND a.ITEM_NO = b.ITEM_NO
					
		]]>
	</select>
	
	<!-- 상품에서 바로 주문 -->
	<select id="selectOne" resultType="hashmap" parameterType="hashmap">
		<![CDATA[ 
			SELECT a.ITEM_NAME, a.ITEM_IMG, a.ITEM_PRICE, a.ITEM_DCPRICE
				FROM PET_ITEM a
					WHERE a.ITEM_NO = #{ITEM_NO}
	    ]]>
	</select>
	
	<!-- 결제정보추가 -->
	<insert id="odInsert" parameterType="hashmap">
		insert into PET_ITEM_ORDER
					 (ORDER_NO,
					  ITEM_NO,
					  ORDER_BUYCOUNT,
					  MEMBER_ID,
					  ORDER_PRICE,
					  ORDER_STATE,
					  ORDER_DATE,
					  ORDER_TYPE,
					  ORDER_IMG)
			values(ORDER_NO_SEQ.NEXTVAL,
					  #{ITEM_NO},
					  #{ORDER_BUYCOUNT},
				      #{MEMBER_ID},
				      #{ORDER_PRICE},
				      #{ORDER_STATE},
				      #{ORDER_DATE},
				      #{ORDER_TYPE},
				      #{ORDER_IMG})
	</insert>
	
	<!-- 펫샵구매취소 -->
	<delete id="odDelete" parameterType="hashmap">
		<![CDATA[
			DELETE FROM PET_ITEM_ORDER WHERE ORDER_NO = #{ORDER_NO}
		]]>
	</delete>
	
	
	<!-- 펫샵구매내역 -->
	<select id="odList" resultType="hashmap" parameterType="hashmap">
		<include refid="selectAll" />
		<include refid="where-id" />
		<![CDATA[ ORDER BY ORDER_NO DESC ]]>
	</select>

</mapper>