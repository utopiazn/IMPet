<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="PetOrderSQL">

	<sql id="seletALL" > 
		<![CDATA[ SELECT * FROM PET_ITEM_BASKET ]]>
	</sql>
	
	<sql id="where-no"> 
		<![CDATA[ WHERE BASKET_NO = #{BASKET_NO}]]> 
	</sql>
	
	<sql id="where-id"> 
		<![CDATA[ WHERE MEMBER_ID = #{MEMBER_ID}]]> 
	</sql>
	
	
	<!-- 장바구니 상품 전체 주문 -->
	<select id="selectAll" resultType="hashmap" parameterType="hashmap">
		<![CDATA[
			SELECT a.ITEM_NAME, a.ITEM_IMG, a.ITEM_PRICE, b.*
				FROM PET_ITEM a, PET_ITEM_BASKET b
					WHERE b.MEMBER_ID = #{MEMBER_ID} AND a.ITEM_NO = b.ITEM_NO
					
		]]>
	</select>
	
	<!-- 상품에서 바로주문 -->
	<select id="selectOne" resultType="hashmap" parameterType="hashmap">
		<![CDATA[ 
			SELECT a.ITEM_NAME, a.ITEM_IMG, a.ITEM_PRICE, b.*
	          FROM PET_ITEM a, PET_ITEM_BASKET b
	            WHERE  b.BASKET_NO = BASKET_NO_SEQ.CURRVAL AND b.MEMBER_ID = #{MEMBER_ID} AND a.ITEM_NO = b.ITEM_NO ]]>
	</select>
	
	<!-- 주문하기 -->
	<insert id="odInsert" parameterType="hashmap">
		insert into PET_ITEM_BASKET
					 (BASKET_NO,
					  ITEM_NO,
					  MEMBER_ID,
					  BASKET_BUYCOUNT)
			values(BASKET_NO_SEQ.NEXTVAL,
					  #{ITEM_NO},
				      #{MEMBER_ID},
				      #{BASKET_BUYCOUNT})
	</insert>
	
	<!-- 주문취소 -->
	<delete id="odDelete" parameterType="hashmap">
		<![CDATA[
			DELETE FROM PET_ITEM_BASKET WHERE BASKET_NO = #{BASKET_NO}
		]]>
	</delete>

</mapper>